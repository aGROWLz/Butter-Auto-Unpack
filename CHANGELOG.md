# 更新日志

## 2026-03-18

### 新增功能

1. **7z 自动格式检测**
   - 添加 `-t*` 参数，支持自动识别压缩格式，不依赖文件扩展名
   - 即使压缩包扩展名被修改（如 `.zip` 改为 `.rar`）也能正确解压

2. **完整版 7z 支持**
   - 优先使用 `7z.exe`（完整版）替代 `7za.exe`（精简版）
   - 新增 RAR 格式支持

3. **递归处理子文件夹**
   - 解压后遇到子文件夹时，自动进入继续检索压缩包
   - 不再因文件夹中有其他文件而停止递归

4. **GUI 解压文件夹显示**
   - 顶部工具栏显示当前解压文件夹路径
   - 添加"📂 打开"按钮，一键打开解压文件夹

5. **文件列表增强**
   - 新增"解压文件夹"列，显示每个文件的解压路径
   - 操作列添加"📂 打开"按钮，快速打开对应文件夹

### 修复问题

1. **修复 Windows 路径分隔符问题**
   - 打开文件夹时自动将 `/` 转换为 `\`
   - 忽略 explorer 命令的非零退出码误报

2. **修复数据库方法名错误**
   - 修正 `get_record` 为 `get_record_by_id`

3. **排除系统文件误识别**
   - `.001.dll`、`.002.exe` 等系统文件不再被识别为伪装压缩包
   - 排除扩展名：`.dll`、`.exe`、`.sys`、`.drv`

4. **修复代码缩进错误**
   - 修正 `recursive_handler.py` 中的缩进问题

### UI 优化

1. **优化复选框样式**
   - 调整复选框大小为 18x18 像素，更加精致
   - 优化边框颜色和粗细，提高可见性
   - 选中状态显示白色对勾图标
   - 减小"选择"列宽度至 40 像素，布局更紧凑

## 2026-03-18（晚）

### 新增功能

1. **集成 Bandizip 解压引擎**
   - 添加 Bandizip 作为可选解压工具，支持更多压缩格式
   - GUI 配置中可选择首选解压软件（Bandizip 或 7-Zip）
   - Bandizip 支持自动检测文件真实格式，不受扩展名影响

2. **自动扩展名切换检测**
   - 当文件无法以当前扩展名解压时，自动尝试其他常见扩展名
   - 支持切换的扩展名：.zip, .rar, .7z, .001, .002 等
   - 智能识别文件真实格式，提高解压成功率

3. **增强状态跟踪系统**
   - 新增 "未解压" 状态：压缩包存在但解压文件夹不存在
   - 新增 "包已删除" 状态：压缩包已删除但解压文件夹存在
   - 新增 "已删除" 状态：压缩包和解压文件夹都不存在
   - 文件移走后移回可自动恢复状态

4. **手动解压功能**
   - "未解压" 状态显示绿色 "▶ 解压" 按钮
   - 点击按钮可手动触发解压操作
   - 支持密码保护的压缩包解压

5. **统一删除确认对话框**
   - 单条记录删除和批量删除使用统一的对话框样式
   - 添加 "同时删除文件（压缩包和解压文件夹）" 复选框
   - 避免重复弹窗，操作更便捷

### 修复问题

1. **修复 QStyle 图标错误**
   - 将不存在的 `SP_Busy` 替换为 `SP_BrowserReload`
   - 新增状态使用正确的系统图标

2. **修复异步解压方法不存在错误**
   - 使用 `threading.Thread` 实现异步解压
   - 避免阻塞 GUI 主线程

3. **修复状态恢复逻辑**
   - 压缩包移回后自动检测并恢复为 "解压成功" 状态
   - 完善四种状态之间的转换逻辑

### UI 优化

1. **状态颜色区分**
   - "未解压"：深橙色，提示需要操作
   - "包已删除"：深蓝色，提示压缩包已丢失
   - "已删除"：深灰色带删除线

2. **操作按钮动态显示**
   - "未解压" 状态显示 "▶ 解压" 按钮（绿色）
   - 其他状态显示 "📂 打开" 按钮（蓝色）
